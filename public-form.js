import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
import { AsYouType, getCountries, getCountryCallingCode, parsePhoneNumberFromString } from 'https://esm.sh/libphonenumber-js@1.11.20/max';

const supabase=createClient('https://pacnegivzgxpanphrnwp.supabase.co','sb_publishable_XG-mi_NVeit5BSco9t9AaQ_pk8CU0QG');
const CLUB_KEY='1850TC1850';
const PHOTO_BUCKET='tanneros-prospect-photos';
const PRIVACY_NOTICE_VERSION='2026-08-19-v1';
const $=id=>document.getElementById(id);
const money=new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN'});
const path=location.pathname.replace(/\/+$/,'')||'/';
const regionNames=new Intl.DisplayNames(['es-MX','es'],{type:'region'});
const phoneStates=new Map();
let publicContext=null;
let selectedPhotoFile=null;
let photoPreviewUrl=null;
let pendingProspectId=null;
let pendingPreparedPhoto=null;
let pendingUploadedPath=null;

const registrationCampaigns={
  '/registro/porteros':{code:'captacion_porteros_2026',registrationType:'goalkeeper',pageTitle:'Captación de Porteros',eyebrow:'CAPTACIÓN TANNERY CITY',heading:'Buscamos porteros',intro:'Llena los datos de tu hijo/a y el club te contactará para agendar su proceso de captación.'},
  '/registro/jugadores':{code:'captacion_jugadores_2026',registrationType:'player',pageTitle:'Captación de Jugadores',eyebrow:'CAPTACIÓN TANNERY CITY',heading:'Buscamos jugadores',intro:'Llena los datos de tu hijo/a y el club te contactará para agendar su proceso de captación.'},
  '/registro/scouting':{code:'scouting_externo_2026',registrationType:'general',pageTitle:'Registro de Talento',eyebrow:'SCOUTING TANNERY CITY',heading:'Registra un talento',intro:'Registra los datos del jugador para que el equipo de Scouting pueda dar seguimiento a su perfil y proceso de visoría.'}
};

const countryPriority=['MX','US','AR'];
const phonePlaceholders={MX:'477 123 4567',US:'210 555 1234',CA:'416 555 1234',AR:'11 2345 6789',ES:'612 345 678'};
function setTitle(t){$('pageTitle').textContent=t;document.title=`${t} · Tannery City`;}
function show(html){$('loading').classList.add('hidden');$('content').classList.remove('hidden');$('content').innerHTML=html;}
function escapePublic(v){return String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}
function publicDate(v){if(!v)return '';const d=new Date(`${String(v).slice(0,10)}T12:00:00`);return Number.isNaN(d.getTime())?'':new Intl.DateTimeFormat('es-MX',{day:'numeric',month:'long',year:'numeric'}).format(d);}
function msg(text,type='error'){const el=$('formMessage');if(!el)return;el.textContent=text;el.dataset.type=type;el.classList.toggle('hidden',!text);}
async function rpc(name,params={}){const {data,error}=await supabase.rpc(name,params);if(error)throw error;return data;}
async function getPublicContext(){if(publicContext)return publicContext;publicContext=await rpc('v2_public_context',{club_key:CLUB_KEY});return publicContext;}
function acceptedAt(){return new Date().toISOString();}
function todayInput(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;}
function flagEmoji(code){return code.toUpperCase();}
function countryName(code){try{return regionNames.of(code)||code}catch{return code}}
function countryRows(){return getCountries().map(code=>({code,name:countryName(code),dial:`+${getCountryCallingCode(code)}`})).sort((a,b)=>{const pa=countryPriority.indexOf(a.code),pb=countryPriority.indexOf(b.code);if(pa!==-1||pb!==-1){if(pa===-1)return 1;if(pb===-1)return -1;return pa-pb;}return a.name.localeCompare(b.name,'es-MX');});}

function phoneField(id,label,{required=true,defaultCountry='MX'}={}){
  return `<div class="phone-widget" data-phone-widget="${id}"><div class="phone-label">${label}${required?' *':''}</div><div class="phone-control"><button id="${id}CountryBtn" class="country-trigger" type="button" aria-haspopup="listbox" aria-expanded="false"></button><input id="${id}" inputmode="tel" autocomplete="tel" ${required?'required':''} maxlength="28" aria-describedby="${id}Hint"></div><div id="${id}Picker" class="country-picker hidden"><div class="country-search-wrap"><input id="${id}CountrySearch" class="country-search" type="search" placeholder="Buscar país o lada…" autocomplete="off"></div><div id="${id}CountryList" class="country-list" role="listbox"></div></div><small id="${id}Hint" class="phone-hint"></small></div>`;
}
function renderCountryList(id,query=''){
  const state=phoneStates.get(id);if(!state)return;
  const q=query.trim().toLocaleLowerCase('es-MX');
  const rows=countryRows().filter(c=>!q||`${c.name} ${c.code} ${c.dial}`.toLocaleLowerCase('es-MX').includes(q));
  const box=$(`${id}CountryList`);if(!box)return;
  box.innerHTML='';
  rows.forEach(c=>{const b=document.createElement('button');b.type='button';b.className='country-option';b.dataset.country=c.code;b.setAttribute('role','option');b.innerHTML=`<span class="country-flag">${flagEmoji(c.code)}</span><span class="country-name">${c.name}</span><strong>${c.dial}</strong>`;b.addEventListener('click',()=>selectCountry(id,c.code));box.appendChild(b);});
  if(!rows.length)box.innerHTML='<div class="country-empty">No encontramos ese país.</div>';
}
function refreshCountryTrigger(id){const state=phoneStates.get(id);if(!state)return;const btn=$(`${id}CountryBtn`);if(!btn)return;btn.innerHTML=`<span>${flagEmoji(state.country)}</span><b>${state.country}</b><span>+${getCountryCallingCode(state.country)}</span><i class="tos-icon tos-icon-chevron" aria-hidden="true"></i>`;$(`${id}`).placeholder=phonePlaceholders[state.country]||'Número local';const hint=$(`${id}Hint`);if(hint&&!$(`${id}`).value)hint.textContent=`${countryName(state.country)} · +${getCountryCallingCode(state.country)}`;}
function selectCountry(id,country){const state=phoneStates.get(id);if(!state)return;state.country=country;const input=$(id);input.value='';closeCountryPicker(id);refreshCountryTrigger(id);input.focus();}
function openCountryPicker(id){document.querySelectorAll('.country-picker:not(.hidden)').forEach(el=>{if(el.id!==`${id}Picker`)el.classList.add('hidden')});const picker=$(`${id}Picker`),btn=$(`${id}CountryBtn`);picker.classList.remove('hidden');btn.setAttribute('aria-expanded','true');$(`${id}CountrySearch`).value='';renderCountryList(id);setTimeout(()=>$(`${id}CountrySearch`)?.focus(),30);}
function closeCountryPicker(id){$(`${id}Picker`)?.classList.add('hidden');$(`${id}CountryBtn`)?.setAttribute('aria-expanded','false');}
function formatNationalPhone(id){const state=phoneStates.get(id),input=$(id);if(!state||!input)return;let digits=input.value.replace(/\D/g,'');const maxDigits=['MX','US','CA','AR'].includes(state.country)?10:15;digits=digits.slice(0,maxDigits);input.value=new AsYouType(state.country).input(digits);const hint=$(`${id}Hint`);hint.classList.remove('bad','good');hint.textContent=`${countryName(state.country)} · +${getCountryCallingCode(state.country)}`;}
function validatePhoneField(id,{silent=false}={}){const state=phoneStates.get(id),input=$(id);if(!state||!input)return null;const raw=input.value.trim();const hint=$(`${id}Hint`);if(!raw){if(!silent){hint.textContent='Escribe el número de WhatsApp.';hint.className='phone-hint bad';}return null;}const parsed=parsePhoneNumberFromString(raw,state.country);if(!parsed?.isValid()){if(!silent){hint.textContent=`Número inválido para ${countryName(state.country)}.`;hint.className='phone-hint bad';}return null;}if(!silent){hint.textContent=parsed.formatInternational();hint.className='phone-hint good';}return parsed.number;}
function requirePhone(id){const value=validatePhoneField(id);if(!value)throw new Error(`Escribe un teléfono válido en ${countryName(phoneStates.get(id)?.country||'MX')}.`);return value;}
function wirePhoneField(id,defaultCountry='MX'){phoneStates.delete(id);phoneStates.set(id,{country:defaultCountry});const input=$(id),btn=$(`${id}CountryBtn`),search=$(`${id}CountrySearch`);refreshCountryTrigger(id);btn.addEventListener('click',e=>{e.stopPropagation();$(`${id}Picker`).classList.contains('hidden')?openCountryPicker(id):closeCountryPicker(id)});search.addEventListener('input',()=>renderCountryList(id,search.value));input.addEventListener('input',()=>formatNationalPhone(id));input.addEventListener('blur',()=>validatePhoneField(id));$(`${id}Picker`).addEventListener('click',e=>e.stopPropagation());}
document.addEventListener('click',()=>phoneStates.forEach((_,id)=>closeCountryPicker(id)));

function privacyBlock(prefix,{imageConsent=false}={}){
  return `<div class="privacy-box span-2"><details><summary>Ver aviso de privacidad</summary><p>Tannery City FC, domicilio Españita #1305, Col. Bugambilias, C.P. 37270, León, Guanajuato, usa los datos del menor y del tutor para su inscripción y control deportivo, administrativo y de cobranza; las fotografías, para control interno. El uso en redes, página web y publicidad es opcional. Puedes revocar tu consentimiento y ejercer tus derechos escribiendo a <strong>tannery.city.1850@gmail.com</strong>.</p></details><label class="check consent-line"><input id="${prefix}DataConsent" type="checkbox" required><span>Autorizo el tratamiento de los datos del menor para el control interno del club. <b>*</b></span></label>${imageConsent?`<label class="check consent-line"><input id="${prefix}ImageConsent" type="checkbox"><span>Autorizo el uso de su imagen en redes, página web y publicidad del club. <small>(Opcional)</small></span></label>`:''}<div class="privacy-version">Aviso de privacidad ${PRIVACY_NOTICE_VERSION}</div></div>`;
}
function photoField(){return `<div class="photo-field span-2"><div class="field-title">Foto del jugador <b>*</b></div><div class="photo-row"><div id="photoPreview" class="photo-preview"><span class="tos-icon tos-icon-user" aria-hidden="true"></span></div><div class="photo-actions"><input id="photoCamera" class="hidden" type="file" accept="image/*" capture="environment"><input id="photoUpload" class="hidden" type="file" accept="image/*"><div class="photo-buttons"><button id="takePhoto" class="secondary" type="button">Tomar foto</button><button id="uploadPhoto" class="secondary" type="button">Subir foto</button><button id="removePhoto" class="ghost" type="button" disabled>Quitar</button></div><small>Obligatoria. Nos permite reconocer a tu hijo/a el primer día. Se guarda solo en el club.</small></div></div></div>`;}
function wirePhotoField(){const camera=$('photoCamera'),upload=$('photoUpload');$('takePhoto').addEventListener('click',()=>camera.click());$('uploadPhoto').addEventListener('click',()=>upload.click());camera.addEventListener('change',()=>selectPhoto(camera.files?.[0]));upload.addEventListener('change',()=>selectPhoto(upload.files?.[0]));$('removePhoto').addEventListener('click',()=>selectPhoto(null));}
function selectPhoto(file){selectedPhotoFile=file||null;pendingPreparedPhoto=null;pendingUploadedPath=null;const preview=$('photoPreview');if(photoPreviewUrl){URL.revokeObjectURL(photoPreviewUrl);photoPreviewUrl=null;}if(!file){preview.innerHTML='<span class="tos-icon tos-icon-user" aria-hidden="true"></span>';preview.classList.remove('has-photo');$('removePhoto').disabled=true;return;}if(!String(file.type||'').startsWith('image/')){msg('Selecciona una imagen válida.');selectedPhotoFile=null;return;}photoPreviewUrl=URL.createObjectURL(file);preview.innerHTML=`<img src="${photoPreviewUrl}" alt="Foto seleccionada">`;preview.classList.add('has-photo');$('removePhoto').disabled=false;msg('');}
function loadImage(file){return new Promise((resolve,reject)=>{const u=URL.createObjectURL(file),img=new Image();img.onload=()=>{URL.revokeObjectURL(u);resolve(img)};img.onerror=()=>{URL.revokeObjectURL(u);reject(new Error('No pudimos leer esa foto. Prueba con otra imagen.'))};img.src=u;});}
function canvasBlob(canvas,type,quality){return new Promise(resolve=>canvas.toBlob(resolve,type,quality));}
async function preparePhoto(file){if(!file)throw new Error('La foto del jugador es obligatoria.');const img=await loadImage(file);const maxSide=1600;const scale=Math.min(1,maxSide/Math.max(img.naturalWidth||img.width,img.naturalHeight||img.height));const canvas=document.createElement('canvas');canvas.width=Math.max(1,Math.round((img.naturalWidth||img.width)*scale));canvas.height=Math.max(1,Math.round((img.naturalHeight||img.height)*scale));canvas.getContext('2d').drawImage(img,0,0,canvas.width,canvas.height);let blob=await canvasBlob(canvas,'image/webp',.82);let ext='webp';if(!blob){blob=await canvasBlob(canvas,'image/jpeg',.82);ext='jpg';}if(!blob)throw new Error('No pudimos preparar la foto. Prueba con otra imagen.');if(blob.size>5*1024*1024){blob=await canvasBlob(canvas,'image/jpeg',.68);ext='jpg';}if(!blob||blob.size>5*1024*1024)throw new Error('La foto es demasiado pesada. Prueba con una imagen más pequeña.');return {blob,ext,mime:blob.type||(`image/${ext==='jpg'?'jpeg':ext}`)};}

function registrationForm(campaign){
  const eyebrow=campaign?.eyebrow||'ÚNETE A TANNERY CITY';
  const heading=campaign?.heading||'Registro Tanner';
  const intro=campaign?.intro||'Un solo registro para comenzar. Dinos si viene como jugador, portero o a una academia y nuestro equipo dará seguimiento desde TannerOS.';
  const preset=campaign?.registrationType||'';
  const typeOptions=[
    ['player','Jugador'],
    ['goalkeeper','Portero'],
    ['general','Academia / otra actividad']
  ].map(([value,label])=>`<option value="${value}" ${preset===value?'selected':''}>${label}</option>`).join('');
  return `<div class="eyebrow">${eyebrow}</div><h2>${heading}</h2><p class="muted">${intro}</p><form id="regForm" class="form-grid registration-form">${photoField()}<label class="span-2">¿Qué tipo de registro es? *<select id="registrationType" required><option value="">Elige una opción</option>${typeOptions}</select></label><label>Nombre del jugador *<input id="firstName" autocomplete="given-name" minlength="2" maxlength="80" required></label><label>Apellidos *<input id="lastName" autocomplete="family-name" minlength="2" maxlength="100" required></label><label>Fecha de nacimiento *<input id="birthDate" type="date" required></label><label>Categoría de interés<select id="category"><option value="">Por definir</option><option>Baby Tanners</option><option>T6</option><option>T8</option><option>T10</option><option>T12</option></select></label><label>¿A qué viene? *<select id="purpose" required><option value="">Elige una opción</option><option>Clase muestra</option><option>Visoría / captación</option><option>Inscripción a la academia</option><option>Información</option></select></label><label>¿Con qué pierna patea? *<select id="dominantFoot" required><option value="">Elige una opción</option><option value="right">Derecha</option><option value="left">Izquierda</option><option value="both">Ambas</option></select></label><label>Nombre del padre/madre/tutor *<input id="guardian" autocomplete="name" minlength="2" maxlength="120" required></label>${phoneField('phone','Teléfono (WhatsApp)')}<label>Correo<input id="email" type="email" autocomplete="email" maxlength="254"></label><label>Escuela del niño/a *<input id="school" minlength="2" maxlength="160" placeholder="Nombre de la escuela" required></label><label>¿Cómo nos conociste? *<select id="source" required><option value="Redes sociales">Redes sociales</option><option value="Recomendación">Recomendación</option><option value="Evento">Evento</option><option value="Cancha">Cancha</option><option value="Escuela">Escuela</option><option value="Volante / QR">Volante / QR</option><option value="Otro">Otro</option></select></label><label id="referralWrap" class="hidden">¿Quién te recomendó? *<input id="referralName" maxlength="120" placeholder="Nombre de quien te recomendó"></label><label class="span-2">Mensaje (opcional)<textarea id="publicMessage" rows="4" maxlength="1000" placeholder="Algo que debamos saber"></textarea></label>${privacyBlock('reg',{imageConsent:true})}<div id="formMessage" class="message hidden span-2"></div><button id="regSubmit" class="primary span-2" type="submit">Enviar registro</button></form>`;
}

async function renderRegistro(campaign=null){setTitle(campaign?.pageTitle||'Registro Tanner');await getPublicContext();show(registrationForm(campaign));wirePhotoField();wirePhoneField('phone','MX');$('birthDate').max=todayInput();const source=$('source'),refWrap=$('referralWrap'),refInput=$('referralName');const toggleReferral=()=>{const on=source.value==='Recomendación';refWrap.classList.toggle('hidden',!on);refInput.required=on;if(!on)refInput.value='';};source.addEventListener('change',toggleReferral);toggleReferral();$('regForm').addEventListener('submit',async e=>{e.preventDefault();msg('');const btn=$('regSubmit');btn.disabled=true;try{if(!selectedPhotoFile&&!pendingPreparedPhoto)throw new Error('La foto del jugador es obligatoria.');if(!$('regDataConsent').checked)throw new Error('Necesitamos la autorización de tratamiento de datos para registrar al menor.');const phone=requirePhone('phone');const registrationType=$('registrationType').value;if(!registrationType)throw new Error('Indica si el registro es de jugador, portero o academia.');if(!pendingPreparedPhoto){btn.textContent='Preparando foto…';pendingPreparedPhoto=await preparePhoto(selectedPhotoFile);}if(!pendingProspectId){btn.textContent='Guardando registro…';pendingProspectId=await rpc('v2_public_register_enhanced',{club_key:CLUB_KEY,first_name:$('firstName').value.trim(),last_name:$('lastName').value.trim(),birth_date:$('birthDate').value,phone,email:$('email').value.trim()||null,guardian_name:$('guardian').value.trim(),category_interest:$('category').value||null,source_campaign:campaign?.code||'registro_general_2026',source_channel:$('source').value,registration_type:registrationType,purpose:$('purpose').value,dominant_foot:$('dominantFoot').value,school_name:$('school').value.trim(),referral_name:$('referralName').value.trim()||null,public_message:$('publicMessage').value.trim()||null,privacy_notice_version:PRIVACY_NOTICE_VERSION,data_consent:$('regDataConsent').checked,image_consent:$('regImageConsent').checked});}if(!pendingUploadedPath){btn.textContent='Subiendo foto…';const ctx=await getPublicContext();pendingUploadedPath=`organizations/${ctx.organizationId}/prospects/${pendingProspectId}/profile.${pendingPreparedPhoto.ext}`;const {error}=await supabase.storage.from(PHOTO_BUCKET).upload(pendingUploadedPath,pendingPreparedPhoto.blob,{contentType:pendingPreparedPhoto.mime,cacheControl:'3600',upsert:false});if(error){pendingUploadedPath=null;throw error;}}btn.textContent='Finalizando…';await rpc('v2_public_attach_prospect_photo',{club_key:CLUB_KEY,prospect_id:pendingProspectId,photo_path:pendingUploadedPath});pendingProspectId=null;pendingPreparedPhoto=null;pendingUploadedPath=null;show(`<div class="success"><div class="success-mark"><span class="tos-icon tos-icon-check" aria-hidden="true"></span></div><h2>Registro recibido</h2><p>La información y la foto quedaron guardadas de forma segura. Administración de Tannery City se pondrá en contacto contigo.</p></div>`);}catch(err){const hasPending=Boolean(pendingProspectId);msg(hasPending?`Tus datos ya quedaron guardados. Falta completar la foto: ${err.message||'intenta nuevamente.'}`:(err.message||'No se pudo enviar el registro.'));btn.disabled=false;btn.textContent=hasPending?'Reintentar foto':'Enviar registro';}});}

async function renderPedido(){setTitle('Pedido Tanner');const products=await rpc('v2_public_products',{club_key:CLUB_KEY});const options=(products||[]).map(p=>`<option value="${p.id}">${p.name} · ${money.format(Number(p.price||0))}</option>`).join('');show(`<div class="eyebrow">TIENDA TANNER</div><h2>Levanta tu pedido</h2><p class="muted">Selecciona el producto y deja tus datos. Administración te contactará para confirmar pago, talla y entrega.</p><form id="orderForm" class="form-grid"><label>Nombre del jugador / cliente<input id="customerName" minlength="2" maxlength="120" required></label>${phoneField('customerPhone','Teléfono (WhatsApp)')}<label>Correo<input id="customerEmail" type="email" maxlength="254"></label><label>Producto<select id="product" required><option value="">Selecciona un producto</option>${options}</select></label><label>Cantidad<input id="qty" type="number" min="1" max="99" value="1" required></label><label>Talla / detalle<input id="notes" maxlength="300" placeholder="Ej. talla 10"></label>${privacyBlock('order')}<div id="formMessage" class="message hidden span-2"></div><button class="primary span-2" type="submit">Confirmar pedido</button></form>`);wirePhoneField('customerPhone','MX');$('orderForm').addEventListener('submit',async e=>{e.preventDefault();msg('');const btn=e.submitter;btn.disabled=true;try{if(!$('orderDataConsent').checked)throw new Error('Necesitamos tu autorización de tratamiento de datos.');const customerPhone=requirePhone('customerPhone');const at=acceptedAt();const result=await rpc('v2_public_order_enhanced',{club_key:CLUB_KEY,customer_name:$('customerName').value.trim(),customer_phone:customerPhone,customer_email:$('customerEmail').value.trim()||null,items:[{product_id:$('product').value,quantity:Number($('qty').value||1)}],notes:$('notes').value.trim()||null,consent:{dataAccepted:$('orderDataConsent').checked,privacyNoticeVersion:PRIVACY_NOTICE_VERSION,acceptedAt:at,source:'public-web'}});show(`<div class="success"><div class="success-mark"><span class="tos-icon tos-icon-check" aria-hidden="true"></span></div><h2>Pedido recibido</h2><p>Tu pedido fue registrado correctamente.</p>${result?.folio?`<div class="folio">${result.folio}</div>`:''}</div>`);}catch(err){msg(err.message||'No se pudo enviar el pedido.');btn.disabled=false;}});}

async function renderProgramas(){
  setTitle('Programas y Eventos');
  const directSlug=new URLSearchParams(location.search).get('program');
  const programs=await rpc('v2_public_programs',{club_key:CLUB_KEY,program_slug:directSlug||null});
  if(!programs?.length){show(`<div class="empty-state"><div class="eyebrow">PROGRAMAS</div><h2>Este registro no está disponible</h2><p class="muted">El evento pudo cerrar su registro o completar el cupo. Escríbenos para más información.</p></div>`);return;}
  if(directSlug&&programs.length===1){show(`<div id="programFormWrap"></div>`);renderProgramForm(programs[0].slug,programs[0]);return;}
  const cards=programs.map(p=>{const price=Number(p.fee)>0?money.format(Number(p.fee)):Number(p.feeWeekly)>0?`${money.format(Number(p.feeWeekly))} por semana`:'Gratuito';const meta=[p.startsOn?publicDate(p.startsOn):null,p.location,p.category].filter(Boolean).join(' · ');return `<article class="program-card public-program-card"><div class="eyebrow">${escapePublic(p.type||'Programa')}</div><h3>${escapePublic(p.name)}</h3>${meta?`<p class="program-public-meta">${escapePublic(meta)}</p>`:''}<p>${escapePublic(p.description||'')}</p><div class="program-public-facts"><strong>${escapePublic(price)}</strong>${p.ageMin!=null||p.ageMax!=null?`<small>Edades: ${p.ageMin??'—'} a ${p.ageMax??'—'} años</small>`:''}</div><button class="primary enroll" data-slug="${escapePublic(p.slug)}">Inscribirme</button></article>`;}).join('');
  show(`<div class="eyebrow">PROGRAMAS Y EVENTOS</div><h2>Inscripciones abiertas</h2><p class="muted">Elige la actividad y registra al participante. Recibirás confirmación de Tannery City.</p><div class="programs">${cards}</div><div id="programFormWrap"></div>`);
  document.querySelectorAll('.enroll').forEach(btn=>btn.addEventListener('click',()=>renderProgramForm(btn.dataset.slug,programs.find(p=>p.slug===btn.dataset.slug))));
}

function renderProgramForm(slug,p){
  const photoRequired=/visor/i.test(p?.type||'');
  const sportsFields=photoRequired?`<div class="form-section span-2"><b>Perfil deportivo</b><span>Información para preparar la evaluación.</span></div><label>Posición principal *<input id="pfPosition" maxlength="80" required placeholder="Portero, defensa, medio, delantero"></label><label>Pierna dominante *<select id="pfDominantFoot" required><option value="">Selecciona</option><option>Derecha</option><option>Izquierda</option><option>Ambas</option></select></label><label>Equipo o academia actual<input id="pfCurrentTeam" maxlength="160"></label><label>Experiencia previa<input id="pfExperience" maxlength="300" placeholder="Clubes, ligas o años jugando"></label>`:`<input id="pfPosition" type="hidden"><input id="pfDominantFoot" type="hidden"><input id="pfCurrentTeam" type="hidden"><input id="pfExperience" type="hidden">`;
  const fee=Number(p?.fee)>0?money.format(Number(p.fee)):Number(p?.feeWeekly)>0?`${money.format(Number(p.feeWeekly))} por semana`:'Actividad gratuita';
  $('programFormWrap').innerHTML=`<div class="subcard program-registration"><div class="program-registration-head"><div><div class="eyebrow">${escapePublic(p?.type||'PROGRAMA')}</div><h2>${escapePublic(p?.name||'Programa')}</h2><p>${escapePublic([p?.startsOn?publicDate(p.startsOn):null,p?.location,p?.category].filter(Boolean).join(' · '))}</p></div><strong>${escapePublic(fee)}</strong></div>${p?.description?`<p class="muted">${escapePublic(p.description)}</p>`:''}<form id="programForm" class="form-grid registration-form">${photoRequired?photoField():''}<div class="form-section span-2"><b>Datos del participante</b><span>Usaremos esta información para identificarlo y organizar el evento.</span></div><label>Nombre *<input id="pfFirst" autocomplete="given-name" minlength="2" maxlength="80" required></label><label>Apellidos *<input id="pfLast" autocomplete="family-name" maxlength="100" required></label><label>Fecha de nacimiento *<input id="pfBirth" type="date" required></label><label>Lugar de residencia *<input id="pfResidence" maxlength="160" placeholder="León, Guanajuato" required></label>${sportsFields}<div class="form-section span-2"><b>Tutor y contacto</b><span>La persona responsable recibirá el seguimiento.</span></div><label>Nombre del padre/madre/tutor *<input id="pfGuardian" autocomplete="name" minlength="2" maxlength="120" required></label>${phoneField('pfPhone','Teléfono (WhatsApp)')}<label>Correo<input id="pfEmail" type="email" maxlength="254"></label><label>¿Cómo se enteraron? *<select id="pfSource" required><option>Redes sociales</option><option>Recomendación</option><option>Escuela</option><option>Evento</option><option>Volante / QR</option><option>Otro</option></select></label><div class="form-section span-2"><b>Emergencia y salud</b><span>Información esencial para cuidar al participante.</span></div><label>Contacto de emergencia *<input id="pfEmergencyContact" maxlength="120" required></label>${phoneField('pfEmergencyPhone','Teléfono de emergencia')}<label>Relación con el participante *<input id="pfEmergencyRelation" maxlength="80" placeholder="Abuela, tío, vecino" required></label><label class="span-2">Alergias, condición médica o medicamento<textarea id="pfMedicalSummary" rows="3" maxlength="700" placeholder="Escribe “Ninguna” si no aplica" required></textarea></label><div class="privacy-box span-2"><label class="check consent-line"><input id="pfParticipation" type="checkbox" required><span>Autorizo la participación del menor en esta actividad. <b>*</b></span></label><label class="check consent-line"><input id="pfFitness" type="checkbox" required><span>Declaro que está en condiciones para realizar actividad física. <b>*</b></span></label><label class="check consent-line"><input id="pfRules" type="checkbox" required><span>Acepto las indicaciones, horarios y dinámica del evento. <b>*</b></span></label></div>${privacyBlock('program',{imageConsent:true})}<div id="formMessage" class="message hidden span-2"></div><button id="programSubmit" class="primary span-2" type="submit">Enviar inscripción</button></form></div>`;
  selectedPhotoFile=null;pendingPreparedPhoto=null;pendingUploadedPath=null;if(photoRequired)wirePhotoField();wirePhoneField('pfPhone','MX');wirePhoneField('pfEmergencyPhone','MX');$('pfBirth').max=todayInput();$('programFormWrap').scrollIntoView({behavior:'smooth',block:'start'});
  $('programForm').addEventListener('submit',async e=>{
    e.preventDefault();msg('');const btn=$('programSubmit');btn.disabled=true;
    try{
      if(photoRequired&&!selectedPhotoFile)throw new Error('La foto del participante es obligatoria para la visoría.');
      if(!$('programDataConsent').checked||!$('pfParticipation').checked||!$('pfFitness').checked||!$('pfRules').checked)throw new Error('Acepta las autorizaciones obligatorias para continuar.');
      const phone=requirePhone('pfPhone'),emergencyPhone=requirePhone('pfEmergencyPhone');const at=acceptedAt();let prepared=null;if(photoRequired){btn.textContent='Preparando foto…';prepared=await preparePhoto(selectedPhotoFile);}
      btn.textContent='Guardando inscripción…';const result=await rpc('v2_public_program_enroll_enhanced',{club_key:CLUB_KEY,program_slug:slug,first_name:$('pfFirst').value.trim(),last_name:$('pfLast').value.trim(),phone,email:$('pfEmail').value.trim()||null,birth_date:$('pfBirth').value,consent:{dataAccepted:$('programDataConsent').checked,imageAccepted:$('programImageConsent').checked,participationAccepted:$('pfParticipation').checked,fitnessAccepted:$('pfFitness').checked,rulesAccepted:$('pfRules').checked,privacyNoticeVersion:PRIVACY_NOTICE_VERSION,acceptedAt:at,source:'public-web'},metadata:{guardianName:$('pfGuardian').value.trim(),residence:$('pfResidence').value.trim(),position:$('pfPosition').value.trim()||null,dominantFoot:$('pfDominantFoot').value||null,currentTeam:$('pfCurrentTeam').value.trim()||null,experience:$('pfExperience').value.trim()||null,emergencyContact:$('pfEmergencyContact').value.trim(),emergencyPhone,emergencyRelation:$('pfEmergencyRelation').value.trim(),medicalSummary:$('pfMedicalSummary').value.trim(),sourceChannel:$('pfSource').value}});
      if(result?.photoRequired&&prepared){btn.textContent='Subiendo foto…';const context=await getPublicContext();const photoPath=`organizations/${context.organizationId}/programs/${result.programId}/${result.id}/profile.${prepared.ext}`;const {error}=await supabase.storage.from(PHOTO_BUCKET).upload(photoPath,prepared.blob,{contentType:prepared.mime,cacheControl:'3600',upsert:false});if(error)throw error;btn.textContent='Finalizando…';await rpc('v2_public_attach_program_photo',{club_key:CLUB_KEY,enrollment_id:result.id,photo_path:photoPath});}
      const wait=result?.status==='waitlisted';show(`<div class="success"><div class="success-mark"><span class="tos-icon tos-icon-check" aria-hidden="true"></span></div><h2>${wait?'Registro en lista de espera':'Inscripción recibida'}</h2><p>${wait?'El cupo está completo. Tannery City te contactará si se libera un lugar.':'Tus datos quedaron vinculados al evento. Tannery City te contactará por WhatsApp para confirmar los siguientes pasos.'}</p><div class="folio">${escapePublic(result?.program||p?.name||'Tannery City')}</div></div>`);
    }catch(err){msg(err.message||'No se pudo enviar la inscripción.');btn.disabled=false;btn.textContent='Enviar inscripción';}
  });
}

try{if(path==='/registro'||registrationCampaigns[path])await renderRegistro(registrationCampaigns[path]||null);else if(path==='/pedido')await renderPedido();else if(path==='/programas')await renderProgramas();else show('<div class="empty-state"><h2>Ruta no disponible</h2></div>');}catch(err){show(`<div class="empty-state"><h2>No pudimos cargar esta página</h2><p class="muted">${err.message||'Intenta nuevamente.'}</p></div>`);}
